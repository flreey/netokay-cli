# @netokay/contracts

Canonical NetOkay EvidenceBundle and Control API contracts, plus generated TypeScript/standalone validator artifacts. Edit only schemas under `schemas/`; distribution copies are generated and drift-checked.
